# cloudflare-render-playground
This repo will be demonstrating a ALWAYS ON website with frontend(html, css, js) using cloudflare pages, edge layer(cloudflare workers) and backend(flask) using render for FREE. 
